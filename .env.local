# Arquivo: .env.local
# (Localizado na raiz do projeto mili-frontend)

# CORREÇÃO: Removido o "/api" do final da URL.
# Esta variável aponta para o endereço base do seu backend em Rust.
NEXT_PUBLIC_API_URL=http://localhost:3000